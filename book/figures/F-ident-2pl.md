**The indeterminacy that must be restricted, Rasch versus 2PL.** Axes index the transformations that leave the likelihood unchanged: shifting persons and items together (horizontal) and rescaling the latent metric while compensating in the discriminations (vertical). (a) Under the Rasch model the discriminations are fixed at one, so only the shift survives and a single location constraint identifies the structure (@sec-ch04). (b) Under the 2PL the whole plane is an orbit of equivalent structures, and the software of this programme pins it with two constraints, mean difficulty zero and mean log discrimination zero (@sec-ch26); fixing the variance of G instead pins the same axis a different way. Conceptual layout; no analysis output appears. Generated by code/R/22-figures-v2-concepts.R.
